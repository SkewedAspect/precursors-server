%% This file was generated by devboot.
%% If you are comfortable editing erlang application configuration scripts
%% there is no harm in editing the file.
[{precursors_server, [

	% define the list of nodes to be used in the precursors cluster.
	% at least one node should be defined.
	{nodes, ['precursors_server@narayana']},
	
	{auth_backends, [
		%{mongo_auth, 2, [{"localhost", 27017}]},
		%{mongo_auth, 1, [{"209.144.228.139", 27017}, {"pre_auth", "current loki green #tube"}]},
		{config_auth, 2, [{allow, [morgul, whitelynx, burst, lordnull]}]}
	]}

]},

{'lager', [
	{handlers, [
		{lager_console_backend, [info,true]},
		{lager_file_backend, [
			{"debug.log", debug, 10485760, "", 5}
		]}
	]},
	{crash_log, "crash.log"}
]},

{sasl, [
	{errlog_type, error} % disable SASL progress reports
]}].

% vim: ft=erlang
